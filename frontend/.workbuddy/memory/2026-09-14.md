# 2026-09-14

## 两项修正（无后端改动）
1. **箭头工具恢复平移视野**：`.canvas-area` 光标改 grab/grabbing；空白处 mousedown → `startViewPan`
   （只动 `.canvas-viewport` 的 scrollLeft/Top，内容坐标不变）；位移 < 4px 视为纯单击才取消选中。
   「移动内容」工具仍独立（优先级更高，返回早于平移分支）。
2. **关系图舒展 + 去无意义连线**：
   - 只画「中心 ↔ 邻居」辐条（`drawerEdgeGeoms` / `edgeGeoms` 都加 `source===focusId || target===focusId`），
     邻居间的弦线横穿圆心、压住中心，等它成为中心自然展开（中心切换式约定）；
   - 丢弃自环（source===target，`extEdges` relation 分支与 `visibleEdges` 都过滤）；
   - 加大间距：独立页 gap 30→46、最低半径 150→190，半径上限修正为
     `min(canvasW, canvasH)/2 - 78`（原来用 canvasW/2-70=420，纵向 620 早溢出）；抽屉 gap 26→42。
- 验证（无头 Edge，用完已清理）：拖空白 `scroll 140→68/92` 与拖动方向相反 ✓；
  关系图 4 节点中心 eaw → 5 条辐条，切中心 efwa → 2 条（原来会把邻居间弦线也画出来）✓；
  选中节点是 prop 不是 class，探针选择器写错属正常。
- `vue-tsc --noEmit` 0 错误；`vite build` ✓ 24.04s。

## 书架排序修复 + 用户资料（第 4 轮）
1. **「按最近编辑」排序修复**：根因是按 `updatedAt` 排，重命名/换封面（PUT /books/{id}）也刷新它。
   前端改为按 `contentUpdatedAt ?? updatedAt`（`bookStore.ts` 导出 `bookContentTime()`，
   排序 + 卡片时间 + 副标题统一走它）；标签「按最近打开」→「按最近编辑」。
   后端字段 `contentUpdatedAt` 写入 `docs/后端接口需求.md` **第十节**（刷新规则表 + curl + prompt）。
2. **用户资料**：头像（书架顶栏，原本硬编码「沈」）点击弹出面板：展示名（默认「用户」）+
   主题预留（light 可选，dark/羊皮纸 disabled 占位）。新增 `api/profile.ts`（独立 axios 实例、
   失败静默返 null，**不走**共享 request 实例避免 404 弹全局错误）+ `stores/userStore.ts`
   （localStorage `wb.profile` 兜底 + applyTheme 写 `documentElement.dataset.theme` 挂载点）。
   后端 GET/PUT /api/profile prompt 写入 **第十一节**。
3. 校验：vue-tsc 0 错、vite build ✓26.5s；Edge headless CDP 实测默认值/改名保存/缓存写入全过。
   坑：Node 原生 WebSocket 只有 addEventListener 没有 .on；genie-trash 删不掉 Edge profile 目录
   （要先 Stop-Process msedge 再 [System.IO.Directory]::Delete）。

## 用户资料改为后端权威存储（第 5 轮）
按沈先生要求，profile 不再是「localStorage 优先」：`userStore.save` 改为先 `PUT /api/profile`
并用**后端返回值**回填，接口未就绪（404/失败返 null）才落 `wb.profile` 降级缓存，功能不中断；
`load` 本来就以后端为准。doc 第十一节「背景」同步改为「后端为唯一权威」。tsc OK + build ✓23.2s。
