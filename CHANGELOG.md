# 更新日志

本文件记录每次发行版本对用户可见的变化。版本号有两个来源，改版本时两处都要改：

| 位置 | 作用 |
| --- | --- |
| `pom.xml`（根）的 `<version>` | 脚本读它做版本报告（`scripts/_common.ps1` 的 `Get-DhaAppVersion`） |
| `electron/package.json` 的 `version` | electron-builder 用它拼产物文件名 |

---

## 1.1.0 — 2026-09-18

画布操作能力补齐，关系图与时间线的视觉打磨。

### 新增

**画布导出为图片**

工具条新增「导出图片」，把当前画布导出为 **2 倍分辨率 PNG**，文件名自动取
`<案件名>·<画布名>.png`。

画布是 SVG 与 HTML 混合渲染的（对象和连线是 SVG，注解是 HTML 富文本，时间线面板是
HTML 里嵌 SVG），所以导出不是单纯挑一张 SVG 序列化 —— 而是把整棵画布克隆成一份独立的
SVG 再栅格化，注解、时间线、背景图案都能完整保留。

导出前会自动回到 100% 缩放，并临时隐藏所有编辑态控件（虚线选中框、缩放手柄、
时间线工具条与把手、吸附辅助线等），所以出来的是一张干净的成品图，
不会把操作痕迹画进去。导出完成后画布恢复原样，当前缩放和选中状态都还在。

**删除对象与注解**

- **对象**：右键弹出菜单（编辑资料 / 删除对象），或选中后按 `Delete` / `Backspace`。
  删除对象时会**连带清掉挂在它身上的关系连线**，确认框里会写清会影响几条关系 ——
  避免数据里留下指向空对象的悬空边。
- **注解**：悬停时头部出现删除按钮。内容为空的直接删掉，有内容才弹确认。

**对象缩放**

选中对象后四角出现手柄，拖动即可**等比缩放**。对角固定（拖哪个角，哪个角跟着动）、
宽高比锁定、4px 网格吸附，边长限制在 48–720 之间。

### 调整

**人物关系图：圆圈收小、间距放开**

| 项目 | 调整前 | 调整后 |
| --- | --- | --- |
| 人物圆圈半径 | 30–46 | **28–40**（2/3/4 字分别 28 / 34 / 40） |
| 相邻环默认间距 | 26 | **34** |
| 独立关系图页外环间距 | 46 | **72** |
| 画布抽屉外环间距 | 42 | **58** |
| 名字字号 | 13.5 / 12.5 | **12 / 11.5** |

圆圈变小、间距拉开之后，环形布局不再挤成一团；名字字号跟着收一档，小圈里也不会溢出。

**时间线的时间段：改成单层细带**

原来用两层矩形叠加表现一个时间段（30px 淡色晕染 + 16px 主笔触），观感像荧光笔涂了两遍。
现在改成**单层 8px 圆角胶囊**，贴着轴线居中，读起来就是「轴线在这一段被加粗」，
干净且不抢视线。水平时间轴和垂直时间轴都改了，图例小样同步。

### 修复

**画布导出报 `Tainted canvases may not be exported`**

导出时把序列化好的 SVG 交给 `<img>` 用的地址是 `URL.createObjectURL()` 生成的 `blob:`
URL。Chromium 会把 `blob:` 载入的图片按**非同源**处理，`drawImage` 之后 canvas 立刻被
标记为污染（tainted），随后的 `toBlob()` 被浏览器直接拒绝：

```
Failed to execute 'toBlob' on 'HTMLCanvasElement': Tainted canvases may not be exported
```

改用 `data:` URL 后这张图被当作同源，导出正常。

值得记一笔的是这个 bug 的**表现具有欺骗性**：被污染的 canvas 仍然能正常显示，
只有 `toBlob()` / `toDataURL()` / `getImageData()` 会被拒。所以画布看着一切正常，
一点导出就报错。这个问题在浏览器里和打包后的 Electron 里都会出现。

---

## 1.0.0 — 2026-09-15

首个发行版。

- **Book → Event → Page → Canvas** 的四层笔记结构：案件下分事件，事件下分页，页上是画布。
- 画布上可摆对象（人物 / 物品 / 地点等）、连线表达关系、写富文本注解。
- 三件扩展视图：**自适应时间线**、**人物关系图**、**族谱图**。
- **完全离线**：不联网、不要账号，数据就是一个 SQLite 文件加一组附件。
- 架构为 **Electron 外壳 + 内嵌 Spring Boot**，前端由后端一并托管，
  所以运行时只有一个进程、一个端口；冷启动到可交互约 **3.3 秒**。
- 发行形态：便携版 zip（解压即用）与 NSIS 安装包（可选安装路径、建快捷方式），
  两者内容一致，包内自带裁剪过的 JRE，**运行不需要装任何环境**。
