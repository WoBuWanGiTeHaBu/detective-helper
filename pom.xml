<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.4</version>
        <relativePath/>
    </parent>

    <groupId>com.theos</groupId>
    <artifactId>detective-helper</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>Detective Helper</name>
    <description>推演录 · 单机桌面应用（Electron 外壳 + 内嵌 Spring Boot + Vue 前端）</description>

    <!--
      模块顺序即 Maven 反应堆顺序，不能随意调换：
        frontend 先产出 dist → backend 把 dist 收进 classpath:static 并打成可执行 jar。
      backend 对 frontend 没有 Maven 依赖（frontend 是 pom 打包），所以顺序只能靠这里保证。

      这里没有 desktop 模块 —— 外壳已经从 JavaFX 换成 Electron（见 electron/）。
      Electron 侧不参与 Maven 构建，它只消费 build/stage-jvm/app/app.jar。
    -->
    <modules>
        <module>frontend</module>
        <module>backend</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <java.version>21</java.version>
        <spring.boot.version>3.3.4</spring.boot.version>
        <lombok.version>1.18.34</lombok.version>
        <mybatis.boot.version>3.0.3</mybatis.boot.version>
        <sqlite.version>3.45.1.0</sqlite.version>
        <jgrapht.version>1.5.2</jgrapht.version>

        <!--
          Windows 上 Java 的 ProcessBuilder 不会按 PATHEXT 补 .cmd 后缀，
          exec-maven-plugin 直接写 "npm" 会找不到可执行文件。下面的 profile 会覆盖成 npm.cmd。
        -->
        <npm.executable>npm</npm.executable>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.theos</groupId>
                <artifactId>backend</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>org.mybatis.spring.boot</groupId>
                <artifactId>mybatis-spring-boot-starter</artifactId>
                <version>${mybatis.boot.version}</version>
            </dependency>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
                <optional>true</optional>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>3.13.0</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>UTF-8</encoding>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>3.3.1</version>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <id>windows</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <properties>
                <npm.executable>npm.cmd</npm.executable>
            </properties>
        </profile>
    </profiles>

</project>
