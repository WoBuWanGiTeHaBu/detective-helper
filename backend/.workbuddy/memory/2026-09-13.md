# 2026-09-13

## 修复后端编译错误（EventService / PageService / RelationGraphService）

现象：`java: 找不到符号 方法 getPageId() 位置 EventCreateDTO`。实际是一整批编译错误。

根因：service 层是按旧数据模型（Page → Event 两级）写的，与实际结构（Book → Event → Page 三级）错位，
DTO / VO / domain 三方字段对不上。`mvn compile` 共暴露出约 110 处 "找不到符号"。

### 三层结构（以 OpenAPI 文档 + schema.sql 为准）
- book ──< event（book_id）──< page（event_id，含 canvas_data）
- book ──< relation_graph（book_id，含 data）
- 前端目录顺序：book → event → page；画布挂在 page 上

### 主要改动
- `EventCreateDTO`：去掉 bookId / sortOrder，只留 name（bookId 走 URL 路径）
- `EventUpdateDTO`：去掉 sortOrder，只留 name
- `PageCreateDTO` / `PageUpdateDTO`：去掉 eventId / sortOrder / canvasData，只留 name
- `RelationGraphCreateDTO` / `RelationGraphUpdateDTO`：去掉 bookId / data，只留 name
- `RelationGraphExtractDTO`：字段改为 `objectTypes` / `relationTypes`（List<String>），去掉 bookId
- 新增 `dto/SortItem`（id + sortOrder），OpenAPI 里批量排序的请求体元素
- `EventService`：createEvent(bookId, dto)、getEventsByBookId、sortEvents(bookId, dto)；页面数走 `countPagesByEventId`
- `PageService`：createPage(eventId, dto)、getPagesByEventId、savePageCanvas（1MB 上限校验）、sortPages
- `RelationGraphService`：createRelationGraph(bookId, dto)、saveRelationGraphData、extractRelationGraph 返回节点/边 JSON
- `RelationGraphDetailVO`：data 原始串之外，新增解析后的 `nodes` / `edges`（List<Map>）
- `BookWorkspaceVO`：按 OpenAPI 重构为 `{book, events[{pages[]}], relationGraphs}`，删掉旧的平铺字段
- Controller 路径全部对齐 OpenAPI：`/api/books/{bookId}/events`、`/api/events/{eventId}/pages`、
  `/api/pages/{id}/canvas`、`/api/books/{bookId}/relation-graphs`、`/api/books/sort`(PUT) 等
- `CanvasController` 不再注册 Bean，画布接口合并进 `PageController`
- `BookService`：注入 Event/Page/RelationGraph 三个 repository，`getBookWorkspace` 组装真实数据，删除案件书时手动级联

### 构建环境备忘（重要）
机器上没有全局 mvn，用 IDEA 自带 Maven 启动，注意路径必须是 **Windows 风格**，Unix 风格 `/d/...` 会
导致 `ClassNotFoundException: plexus.classworlds.launcher.Launcher`：

```bash
export PATH="/d/JAVA/jdk/bin:$PATH"; export JAVA_HOME="D:/JAVA/jdk"
M3="D:/JAVA/IDEA/IntelliJ IDEA 2026.1.2/plugins/maven/lib/maven3"
java -cp "$M3/boot/plexus-classworlds-2.9.0.jar" \
  "-Dclassworlds.conf=$M3/bin/m2.conf" "-Dmaven.home=$M3" \
  "-Dmaven.multiModuleProjectDirectory=E:/IdeaProjects/detective-helper" \
  org.codehaus.plexus.classworlds.launcher.Launcher -f "E:/IdeaProjects/detective-helper/backend/pom.xml" package -DskipTests
```
另有两点：Git Bash 被 WorkBuddy shim 干扰，`ls/tail/grep` 需显式把 PortableGit 的 bin/usr/bin 加进 PATH；
`grep` 读 Maven 输出要加 `-a`（含中文被当作二进制）。

结果：`mvn compile` 与 `mvn package` 均 BUILD SUCCESS，产物 `target/backend-1.0.0-SNAPSHOT.jar`。

### 待办
- `extractRelationGraph` 仍是占位实现，待接入 `graph/RelationExtractor`
- 书籍封面上传（POST /api/books/{id}/cover）、Result.code 与文档示例（0 / 20001 等）仍不一致

---

## 排查「5173 请求不到后端」+ 修正后端端口

现象：前端 dev server（5173）请求 `/api/*` 拿不到后端。

### 三个叠加的原因（按发现顺序）
1. **`server.port: 0`**——后端随机端口。实测三次分别是 58472 / 58074 / 58074，
   每次重启都变，vite proxy 的固定 target 永远指不到。
2. **8080 被本机 nginx 占用**（`netstat` 确认 PID 是 nginx.exe）。所以即使把后端
   指到 8080 也会冲突；此前 curl 8080 收到的 502 错误页是 **nginx 的**，不是 Spring 的，
   这点很容易误判成「后端崩了」。
3. **WorkBuddy 注入了 `SERVER__PORT=58074`**（注意是**双下划线**）。Spring Boot 的
   relaxed binding 会把 `SERVER__PORT` 归一化成 `server.port`，于是它覆盖了
   application.yml。这也是为什么排查中一度出现「日志显示端口 58074 但配置写的 8081」。
   清环境变量要用 `env -u SERVER__PORT -u SERVER__HOST`，光 `-u SERVER_PORT` 无效。
   此条属于我这边的环境干扰，**不影响用户正常启动**（IDEA 里没有这个变量）。

### 改动
- `application.yml`：`server.port: 0` → `8081`，并加注释说明必须与 vite proxy 一致、
  以及不用 8080 的原因
- `vite.config.ts`：`/api` 与 `/files` 的 proxy target 8080 → 8081
- `utils/coverUrl.ts`：注释里的端口同步
- `api/request.ts`：网络错误文案不再写死 `localhost:8080`（正是它把人带偏了），
  改为按 DEV 环境提示「确认后端已启动且端口与 proxy 一致」
- `WebConfig`：`allowedOriginPatterns("*")` 收紧为 `http://localhost:[*]` /
  `http://127.0.0.1:[*]`，并补 `maxAge(3600)`，注释说明 allowCredentials 与
  allowedOrigins("*") 互斥的坑

### 结论：原 CORS 配置**本身没写错**，只是从没被用上
dev 下走 vite proxy，浏览器眼里是同源（都在 5173），压根不触发 CORS。
`allowedOriginPatterns("*")` + `allowCredentials(true)` 是合法组合（用 allowedOrigins 才会报错）。
改它纯粹是生产环境收紧需要，不是故障原因。

### 验证结果（实测）
- `Tomcat started on port 8081`，直接 curl 接口通过
- CORS 预检：200，`Access-Control-Allow-Origin: http://localhost:5173`、
  `Allow-Credentials: true`、`Allow-Methods`/`Allow-Headers`/`Max-Age` 齐全
- POST `/api/books` → `{"code":200,...,"data":{"id":1,...}}`，业务链路通

### 环境坑备忘（继续记）
- curl 要加 `--noproxy '*'` 并 unset `http_proxy/https_proxy`（本机有 51877 代理），
  否则本地请求会被代理吞掉
- 后台 `&` 起的 java 进程会随 shell 结束被回收，验证要写在同一个 Bash 调用里
  （起服务 → sleep → curl → kill）


---

## 按 docs/后端接口需求.md 完成 5 项改动（验证全过）

1. **Timeline x/y/width/height**：`CanvasResponse.Timeline` 原有 x/y，本轮补 `width`/`height`（Double，可空）。
   整块 JSON 存 `page.canvas_data`，天然往返，无需动 schema。
2. **关系不去重**：确认保存路径是「整个画布 JSON 序列化后整体覆盖 canvas_data」，从不按 source+target 去重；
   验证脚本里 r1(o1→o2) 与 r2(o2→o1) 同对双向并存读回。
3. **无损往返确认**：`Extensible` 基类 `@JsonAnySetter/Getter` 兜未知字段；style/customFields 原样；
   annotation.content 直接字符串存取（HTML 的 style 属性读回仍在）。
4. **extract 实现**：`RelationGraphService.extractRelationGraph` 从占位改为真实实现——
   `PageRepository.findByBookId`（JOIN event）遍历书下所有页画布，对象→nodes（type 透传 person/event/thing），
   关系→edges；objectTypes/relationTypes 空数组=不过滤；端点被过滤的关系丢弃防悬空边；跨页按对象 id 去重。
   注意：`graph/RelationExtractor` 是从文本正则提实体的（type=person/time/location），与画布提取是两回事，没有用它。
5. **OpenAPI 文档同步**（`D:\文本\推理笔记 API.openapi.json`）：成功码全部 0→200、message success→操作成功；
   错误码 20001-20004→1001-1004、10001→400；PUT /canvas 响应 schema 从 ResultVoid 改为 ResultCanvasResponse；
   Timeline schema 补 x/y/width/height；CanvasResponse schema 补 background。
   **保留**：`/api/books/{id}/cover` 的 10002/10003 示例没改——该接口后端尚未实现，没有「实际值」可同步。

### 验证（后端 8081 + node fetch 脚本，14 项断言全过）
- PUT 带 grid 背景 + 四字段时间线 + 双向双关系 + 富文本注解 → GET 读回全部原样
- 空数组清空后四数组=[]
- extract(objectTypes=[person]) 只返回 2 个 person 节点、悬空边被丢弃、edge.type=unidirectional
- 编译 `mvn package -DskipTests` BUILD SUCCESS

### 环境坑（新）
- **Git Bash 传中文 JSON 会坏**：`curl -d '{"name":"中文"}'` 的中文经 shell 编码损坏，后端报
  「请求体不是合法的 JSON 对象」（新增的 HttpMessageNotReadableException 处理器生效，400 而非 500）。
  解法：接口验证改用 node `fetch` 脚本（Write 工具写 .mjs 文件保证 UTF-8），shell 只负责起停服务。
- **后台 java 进程残留占端口**：`&` 起的 java 即使 `kill $!` 后仍可能活着占 8081，下次启动报
  Port already in use 而 readiness 检查却打在旧进程上（测的是旧代码）。起服务前先
  `netstat -ano | grep :8081 | grep LISTENING` 找 PID `taskkill //F //PID` 清干净，跑完也用 taskkill 收尾。

### extract 二次调整（按用户复述的 6 条需求）
- 用户反馈「extract 返回成功但数据为空」。两个原因：① 残留旧进程跑着占位实现（8081 端口坑）；
  ② 上一版实现会把「端点不在节点集」的边丢掉——文档第五节脚本的数据 r1/r2 引用了 objects 里不存在的 o2，
  这类数据边会被全丢。已改为**纯映射**：objects→nodes（id 原样）、relationships→edges 原样保留，
  只按 objectTypes/relationTypes 过滤（空=不过滤），edges 数量恒等于画布 relationships 数量。
- 验证脚本（node fetch，13 断言全过）：文档脚本原数据 r1/r2 同时读回；extract edges=2 且 source/target
  含 o2 原样；GraphNode.id === 'o1'；清空后四数组=[]；空书 extract 返回 {nodes:[],edges:[]} 不 500；
  同对同向 2 条关系（unidirectional+bidirectional）全量提取都保留。

---

## 新增「族谱图」接口（与关系图完全同构）

照 RelationGraph 全套复制改名，6 个接口：
- GET/POST /api/books/{bookId}/family-trees（列表 / 创建，新建 data 初始化为 {"members":[],"relations":[]}）
- GET/PUT/DELETE /api/family-trees/{id}（详情 / 改名 / 删除）
- PUT /api/family-trees/{id}/data（@RequestBody String 原样存，整体覆盖、不去重不清洗不解析日期）

新增文件：domain/FamilyTree、repository/FamilyTreeRepository、dto/FamilyTreeCreateDTO、
dto/FamilyTreeUpdateDTO、vo/FamilyTreeVO、vo/FamilyTreeDetailVO（顶层 members/relations + data 原始字符串，
与 RelationGraphDetailVO 形态一致）、service/FamilyTreeService、controller/FamilyTreeController。

连带改动：
- ErrorCode 加 FAMILY_TREE_NOT_FOUND(1008, "族谱图不存在")
- schema.sql 加 family_tree 表（SQLite 方言 INTEGER PK / TEXT 时间戳，data DEFAULT '{"members":[],"relations":[]}'）+ idx_family_tree_book
- BookService 注入 FamilyTreeRepository，deleteBook 级联删 family_tree

验证（node fetch 脚本 25 断言全过）：六接口全通；详情顶层 members 3 / relations 2 且 data 字符串可解析；
保存后重 GET 逐字段一致（gender/birth/death/note/两条关系类型原样）；新建后详情为 [] 非 null（防「重开空图」）；
1008 错误码；删书级联。画布 6 项回归（timeline 四字段、关系不去重、extract、无损往返、清空、code=200）全过。

注意：OpenAPI 文档（D:\文本\推理笔记 API.openapi.json）尚未加入族谱图六个端点，用户没要求，待前端要文档时补。

## CanvasResponse 顶层补 canvasWidth / canvasHeight（固定尺寸纸张）

前端画布改固定尺寸纸张，尺寸要随 PUT/GET /api/pages/{id}/canvas 原样往返。顶层类只有
@JsonIgnoreProperties（会静默丢未知字段），没有 Extensible any-setter 兜底，所以显式加字段：
`private Double canvasWidth / canvasHeight` + getter/setter，仅此而已——不动 normalize()、不加校验、不给默认值。
验证：PUT/GET 均带 "canvasWidth":800.0,"canvasHeight":600.0（Double 序列化带 .0，JS 解析后就是 800/600）；
老画布（无字段）PUT/GET 正常，读回时这两个键为 null。

## 实现 POST /api/books/{id}/cover 封面上传（此前是待办）

前端报「No static resource api/books/3/cover」= 端点不存在，Spring 落到静态资源处理器。
本轮补齐：
- ErrorCode：COVER_FORMAT_NOT_SUPPORTED(10002)、COVER_TOO_LARGE(10003)（与 OpenAPI 文档对齐）
- BookService.uploadCover：书存在校验(1001)→大小≤2MB(10003)→Content-Type 优先/扩展名兜底
  校验 jpeg/png/webp(10002)→存 data/covers/book_{id}_{时间戳}.{ext}（带时间戳防浏览器缓存旧封面）→
  book.coverType="image"、coverValue="covers/xxx"→删旧封面文件
- BookController：POST /{id}/cover，@RequestParam("file") MultipartFile，返回 {"coverValue": ...}
- WebConfig.addResourceHandlers：GET /files/covers/** → file:data/covers/
  （只开放 covers 子目录，不暴露整个 data 目录——里面有 SQLite 库文件）

验证 7 项全过：PNG 上传返回 coverValue；GET /files/covers/xxx.png → 200 image/png；
书籍 coverType/coverValue 正确；重传后旧文件删除；txt 冒充→10002；3MB→10003；书不存在→1001。
前端 vite proxy /files 已存在（vite.config.ts:25），coverUrl() STATIC_BASE='/files' 无需改动。

## 修「传封面时报案件书名称不能为空、但刷新后又成功」

根因：前端传封面成功后补发 PUT /books/{id}（body 只有 coverType/coverValue，不带 name），
BookUpdateDTO.name 挂 @NotBlank → 补丁请求 400 弹错；封面文件本身已传成功所以刷新后正常。
连带发现 updateBook 是「全字段覆盖」：setName(dto.getName()) 无条件执行——若去掉校验不改语义，
封面补丁会把书名清成 null；工作区只改名又会把图片封面 coverValue 抹掉。

改动（PATCH 语义：null = 不修改，与 OpenAPI UpdateBookRequest 全字段 nullable 对齐）：
- BookUpdateDTO 去掉 @NotBlank
- BookService.updateBook：name/coverType/coverValue/coverText 四字段各自判 null 才 set；
  name 显式传空白才报 400「案件书名称不能为空」
- 前端两处对话框 coverText 由 `trim() || null` 改为 `trim()`（null 现在表示不改，清空文案要发空串）：
  BookshelfView.vue 提交对话框、BookWorkspaceView.vue submitBook

验证 4 场景全过：封面补丁不改名；只改名不抹封面；显式空白名 400；全字段更新正常。

## contentUpdatedAt（书架按内容编辑排序）+ /api/profile（用户资料）

### contentUpdatedAt
- schema：book 加 content_updated_at TEXT（可空）；**老库迁移**在 SqliteConfig.migrateColumns()
  （查 pragma_table_info 缺列则 ALTER TABLE ADD COLUMN；CREATE TABLE IF NOT EXISTS 不会给已存在的表加列，
  SQLite 的 ALTER 也没有 IF NOT EXISTS）。服务能对旧库正常启动即迁移生效。
- Book/BookVO 加 String contentUpdatedAt（ISO 字符串，与项目其余时间字段同风格，没用 LocalDateTime）。
- BookRepository.touchContent(bookId)：只 UPDATE content_updated_at，不碰 updated_at。
- 收口在 BookService.touchContent(bookId)，调用方：EventService（create/update/delete/sort）、
  PageService（create/update/delete/saveCanvas/sort×2，私有 touchBookByEvent 经 event 映射回 book）、
  RelationGraphService（create/update/delete/saveData/extract）、FamilyTreeService（create/update/delete/saveData）。
  不调用：updateBook、uploadCover、books/sort。
- 三个读接口（GET /books、/books/{id}、/workspace 嵌套 book）都带出，老数据 null。
- 验证：改名/换封面/排序后不变（updatedAt 照旧刷新）；写画布/改事件/改页面/图数据都变新；21 断言全过。

### /api/profile（单机单用户）
- user_profile 表固定 id=1 一行；UserProfileRepository.find/upsert（UPDATE 无行则 INSERT）。
- GET 无记录返回默认 {displayName:"用户", theme:"light"} 不 404；PUT PATCH 语义只传的才改，
  displayName trim 非空且 ≤24 字否则 400，theme 字符串原样存取不枚举。
- ProfileController + ProfileService + ProfileVO + ProfileUpdateDTO。验证后已把资料还原为默认值。
